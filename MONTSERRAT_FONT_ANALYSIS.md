# Анализ проблемы с шрифтом Montserrat

**Дата анализа**: 2026-01-15  
**Проблема**: Шрифт Montserrat не отображается на сайте

---

## 🔍 Анализ проблемы

### 📍 Текущее состояние

**Использование шрифта**:
- ✅ Montserrat используется в стилях (`font-family:Montserrat`)
- ✅ Шрифт указан в конфигурации проекта как Google Fonts
- ❌ Шрифт не загружается/не отображается

**Конфигурация шрифта** (из `window.ServerData`):
```json
{
  "provider": "google",
  "css_name": "Montserrat",
  "name": "Montserrat",
  "variations": ["n4", "n6", "n7", "n2"]
}
```

**Использование в коде**:
- `snippets/3.html` - множество использований `font-family:Montserrat`
- `snippets/4.html` - множество использований `font-family:Montserrat`
- `index.html` - в стилях текста: `"fontFamily":"Montserrat"`

---

## 🎯 Возможные причины проблемы

### 1. ❌ Отсутствует загрузка через Google Fonts API

**Проблема**: Шрифт должен загружаться через Google Fonts API, но ссылка может отсутствовать или быть неправильной.

**Проверка**:
- В `index.html` не найдена явная ссылка на `fonts.googleapis.com` для Montserrat
- Readymag обычно загружает Google Fonts динамически через JavaScript

### 2. ❌ Content Security Policy (CSP) блокирует загрузку

**Проблема**: CSP может блокировать загрузку шрифтов с `fonts.googleapis.com`.

**Текущий CSP** (из `index.html`):
```html
<meta http-equiv="Content-Security-Policy" 
      content="default-src 'self'; 
               script-src 'self' 'unsafe-inline' 'unsafe-eval' https://st-p.rmcdn1.net https://*.rmcdn1.net; 
               style-src 'self' 'unsafe-inline' https://st-p.rmcdn1.net https://*.rmcdn1.net; 
               img-src 'self' data: https:; 
               font-src 'self' data: https:; 
               connect-src 'self' https://st-p.rmcdn1.net https://*.rmcdn1.net;"/>
```

**Анализ**:
- ✅ `font-src 'self' data: https:` - разрешает загрузку с любого HTTPS источника
- ✅ Должно работать с `fonts.googleapis.com`

### 3. ❌ Динамическая загрузка шрифтов не работает

**Проблема**: Readymag использует динамическую загрузку шрифтов через JavaScript, которая может не работать.

**Логика загрузки** (из `dist/c/c-HF75YENI.js`):
- Шрифты загружаются через функцию `appendFontsCssToDocument`
- Для Google Fonts создается ссылка: `https://fonts.googleapis.com/css?family=...`
- Ссылка должна динамически добавляться в `<head>`

### 4. ❌ Проблемы с кэшированием или CDN

**Проблема**: Google Fonts CDN может быть недоступен или заблокирован.

---

## ✅ Решения

### Решение 1: Добавить явную ссылку на Google Fonts (РЕКОМЕНДУЕТСЯ)

**Действие**: Добавить прямую ссылку на Google Fonts API в `<head>` секцию `index.html`.

**Код для добавления**:
```html
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@200;400;600;700&display=swap" rel="stylesheet">
```

**Где добавить**:
- В `<head>` секцию `index.html`
- После существующих `<link rel="icon">` тегов
- До загрузки JavaScript файлов

**Объяснение весов**:
- `wght@200` = normal 200 (n2)
- `wght@400` = normal 400 (n4)
- `wght@600` = normal 600 (n6)
- `wght@700` = bold 700 (n7)
- `display=swap` - для предотвращения FOIT (Flash of Invisible Text)

### Решение 2: Обновить CSP для явного разрешения Google Fonts

**Текущий CSP** уже разрешает `https:` для шрифтов, но можно добавить явно:

```html
font-src 'self' data: https://fonts.googleapis.com https://fonts.gstatic.com;
```

**Но это не обязательно**, так как `https:` уже покрывает все HTTPS источники.

### Решение 3: Проверить консоль браузера на ошибки

**Действие**: Открыть сайт в браузере, открыть DevTools (F12), проверить:
1. Вкладка **Console** - есть ли ошибки загрузки шрифтов
2. Вкладка **Network** - загружается ли файл с `fonts.googleapis.com`
3. Вкладка **Elements** - есть ли `<link>` тег для Google Fonts

**Что искать**:
- Ошибки типа `Failed to load font`
- Ошибки типа `Blocked by CSP`
- 404 ошибки для `fonts.googleapis.com/css?family=Montserrat`

---

## 🔧 Пошаговая инструкция по исправлению

### Шаг 1: Найти место для вставки в `index.html`

**Расположение**: В `<head>` секции, после `<link rel="manifest">` и до закрывающего `</head>`.

**Пример местоположения**:
```html
<link rel="manifest" href="/manifest.json"/>
<!-- ДОБАВИТЬ ЗДЕСЬ -->
<link rel="next" href="https://unitedcryptoboys.online/2/"/>
```

### Шаг 2: Добавить ссылки на Google Fonts

**Код для вставки**:
```html
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@200;400;600;700&display=swap" rel="stylesheet">
```

**Зачем `preconnect`**:
- Ускоряет загрузку шрифтов
- Устанавливает ранние соединения с доменами Google Fonts
- Улучшает производительность

### Шаг 3: Проверить результат

1. ✅ Открыть сайт в браузере
2. ✅ Открыть DevTools (F12)
3. ✅ Проверить вкладку Network - должен быть запрос к `fonts.googleapis.com`
4. ✅ Проверить, что текст отображается шрифтом Montserrat

---

## ⚠️ Важные замечания

### Ограничения Readymag:

1. **Динамическая загрузка**: Readymag может перезаписывать ссылки на шрифты
2. **Регенерация файлов**: При экспорте из Readymag изменения могут быть потеряны
3. **Рекомендация**: Добавить ссылку на Google Fonts в исходный проект Readymag

### Альтернативное решение:

Если проблема не решается через `index.html`, нужно:
1. Открыть проект в Readymag
2. Проверить настройки шрифтов
3. Убедиться, что Montserrat правильно подключен
4. Переэкспортировать проект

---

## 📝 Резюме

**Проблема**: Шрифт Montserrat не загружается, хотя указан в конфигурации.

**Причина**: Отсутствует явная ссылка на Google Fonts API в HTML.

**Решение**: Добавить ссылку на Google Fonts в `<head>` секцию `index.html`:
```html
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@200;400;600;700&display=swap" rel="stylesheet">
```

**Критичность**: 🔴 Высокая (шрифт не отображается на сайте)

---

*Дата создания анализа: 2026-01-15*  
*Версия: 1.0*
