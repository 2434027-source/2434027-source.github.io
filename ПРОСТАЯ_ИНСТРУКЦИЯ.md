# 🎯 ПРОСТАЯ ИНСТРУКЦИЯ: Куда что вставлять

**Важно**: НЕ загружайте файлы! Просто копируйте код из файлов и вставляйте в Readymag.

---

## 📋 Что делать:

### 1️⃣ Секция `<HEAD>` (первая вкладка)

**Что делать**:
1. Откройте файл **`readymag-head-code.html`** на компьютере
2. Выделите ВЕСЬ код (Ctrl+A)
3. Скопируйте (Ctrl+C)
4. В Readymag откройте вкладку **`<HEAD>`**
5. Вставьте код (Ctrl+V)
6. Нажмите **"Save & Close"**

**Файл**: `readymag-head-code.html`  
**Вкладка**: `<HEAD>`

---

### 2️⃣ Секция `After <BODY>` (вторая вкладка)

**Что делать**:
1. Откройте файл **`readymag-body-code.html`** на компьютере
2. Выделите ВЕСЬ код (Ctrl+A)
3. Скопируйте (Ctrl+C)
4. В Readymag откройте вкладку **`After <BODY>`**
5. Вставьте код (Ctrl+V)
6. Нажмите **"Save & Close"**

**Файл**: `readymag-body-code.html`  
**Вкладка**: `After <BODY>`

---

### 3️⃣ Секция `CSS` (четвертая вкладка)

**Что делать**:
1. Откройте файл **`readymag-css-code.css`** на компьютере
2. Выделите ВЕСЬ код (Ctrl+A)
3. Скопируйте (Ctrl+C)
4. В Readymag откройте вкладку **`CSS`**
5. Вставьте код (Ctrl+V)
6. Нажмите **"Save & Close"**

**Файл**: `readymag-css-code.css`  
**Вкладка**: `CSS`

---

## ❌ НЕ нужно:

- ❌ Загружать файлы через File Manager
- ❌ Использовать вкладку `Before </BODY>` (оставляем пустой)
- ❌ Дублировать код несколько раз

---

## ✅ После вставки:

1. Нажмите **"Save & Close"** в каждой секции
2. Экспортируйте проект из Readymag
3. Готово! Код будет автоматически добавлен в экспортированные файлы

---

## 📁 Соответствие файлов и вкладок:

| Файл на компьютере | Вкладка в Readymag |
|-------------------|-------------------|
| `readymag-head-code.html` | `<HEAD>` |
| `readymag-body-code.html` | `After <BODY>` |
| `readymag-css-code.css` | `CSS` |
| `sw.js` | ❌ НЕ вставлять (загрузить отдельно после экспорта) |

---

## 🔍 Проверка:

После экспорта откройте `index.html` и проверьте:
- В `<head>` должен быть код из `readymag-head-code.html`
- После `<body>` должен быть код из `readymag-body-code.html`
- В CSS должен быть код из `readymag-css-code.css`

---

**Всё просто**: Копировать → Вставить → Сохранить! 
