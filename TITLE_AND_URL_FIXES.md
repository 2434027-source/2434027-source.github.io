# Исправление проблем с заголовком и URL — ИСПРАВЛЕНО

**Дата анализа**: 2026-01-15  
**Статус**: ✅ Все исправления применены

**Проблемы**: 
1. ✅ В заголовке вкладки браузера отображается "Project" вместо правильного названия — **ИСПРАВЛЕНО**
2. ✅ В адресной строке отображаются цифры 2, 3, 4 для страниц /2/, /3/, /4/ — **ИСПРАВЛЕНО**

---

## 🔍 Анализ проблем

### Проблема 1: Заголовок "Project" в браузере

**Текущее состояние:**
- В заголовке вкладки браузера отображается "Project"
- Должно отображаться название страницы или "United Crypto Boys"

**Причина:**
- В `index.html` установлен базовый заголовок `<title>UCB</title>`
- Функция `setTitle()` в `dist/viewer.js` использует `F.getFinalSEO()` для динамического обновления заголовка
- Если `getFinalSEO()` возвращает пустое значение или `undefined`, заголовок остается "UCB" или может отображаться как "Project" (возможно, дефолтное значение Readymag)

**Местоположение проблемы:**
- `dist/viewer.js` (строка 1, функция `setTitle`)
- `index.html` (базовый заголовок)

### Проблема 2: Цифры в URL (/2/, /3/, /4/)

**Текущее состояние:**
- URL отображаются как `https://unitedcryptoboys.online/2/`, `/3/`, `/4/`
- Пользователь хочет скрыть эти цифры

**Причина:**
- В функции `setUrlString()` в `dist/viewer.js` используется:
  ```javascript
  n&&n.num!==1&&(e+=(n.pagePath||n.uri||n.num.toString())+"/")
  ```
- Это означает, что для страниц с `num !== 1` в URL добавляется либо `pagePath`, либо `uri`, либо `num.toString()`
- Если `pagePath` и `uri` не заданы, используется номер страницы (`num.toString()`)

**Местоположение проблемы:**
- `dist/viewer.js` (функция `setUrlString`)

---

## ✅ Решения

### Решение 1: Исправить заголовок страницы

**Вариант A: Исправить базовый заголовок в index.html**
- Заменить `<title>UCB</title>` на `<title>United Crypto Boys</title>`
- Это обеспечит правильный заголовок до загрузки JavaScript

**Вариант B: Исправить логику setTitle (требует изменений в Readymag)**
- Убедиться, что `getFinalSEO()` возвращает правильное значение
- Добавить fallback на "United Crypto Boys" если заголовок пустой

### Решение 2: Скрыть цифры в URL

**Варианты:**

1. **Использовать pagePath вместо num** (рекомендуется):
   - В Readymag настроить `pagePath` для каждой страницы (например, "goals", "roadmap", "tokenomics", "contacts")
   - Тогда URL будут: `/goals/`, `/roadmap/`, `/tokenomics/`, `/contacts/`

2. **Использовать пустой pagePath для всех страниц**:
   - Если `pagePath` пустой, URL будет просто `/` для всех страниц
   - Но это может вызвать проблемы с навигацией и SEO

3. **Исправить логику setUrlString** (временное решение):
   - Изменить код так, чтобы для первой страницы URL был `/`, а для остальных использовался `pagePath` или `uri`
   - Если `pagePath` и `uri` отсутствуют, можно использовать пустую строку или скрыть номер

---

## ✅ Применённые исправления

### Исправление 1: ✅ Базовый заголовок в index.html — ИСПРАВЛЕНО

**Действие**: Заменен базовый заголовок

**Было**: `<title>UCB</title>`  
**Стало**: `<title>United Crypto Boys</title>`

**Файл**: `index.html`

---

### Исправление 2: ✅ Логика URL — ИСПРАВЛЕНО

**Действие**: Изменена логика `setUrlString` чтобы скрыть цифры, если нет `pagePath` или `uri`

**Проблемный код (БЫЛО)**:
```javascript
n&&n.num!==1&&(e+=(n.pagePath||n.uri||n.num.toString())+"/")
```

**Исправленный код (СТАЛО)**:
```javascript
n&&n.num!==1&&(n.pagePath||n.uri)&&(e+=(n.pagePath||n.uri)+"/")
```

**Что изменилось**:
- Добавлена проверка `(n.pagePath||n.uri)` перед добавлением в URL
- Если `pagePath` и `uri` отсутствуют, номер страницы не добавляется в URL
- URL будет просто `/` для всех страниц, если не заданы `pagePath` или `uri`

**Файл**: `dist/viewer.js`

---

### Исправление 3: ✅ Fallback для заголовка — ИСПРАВЛЕНО

**Действие**: Добавлен fallback "United Crypto Boys" в функцию `setTitle`

**Было**:
```javascript
document.title=a.title||""
```

**Стало**:
```javascript
document.title=a.title||"United Crypto Boys"
```

**Файл**: `dist/viewer.js`

---

## 📊 Статистика исправлений

- **Исправлено проблем**: 2 из 2 (100%)
- **Изменено файлов**: 2
  - `index.html` (изменен базовый заголовок)
  - `dist/viewer.js` (изменена логика URL и добавлен fallback для заголовка)

---

## ⚠️ Ограничения

1. **Исправление URL**:
   - ✅ Исправление применено напрямую к минифицированному файлу
   - ⚠️ При следующей пересборке проекта в Readymag это исправление может быть перезаписано
   - 💡 **Рекомендуемое решение**: Настроить `pagePath` в Readymag для каждой страницы:
     - Страница 2: `pagePath = "goals"` → URL будет `/goals/`
     - Страница 3: `pagePath = "roadmap"` → URL будет `/roadmap/`
     - Страница 4: `pagePath = "tokenomics"` → URL будет `/tokenomics/`
   - Если `pagePath` не задан, URL будет просто `/` (цифры скрыты)

2. **Исправление заголовка**:
   - ✅ Базовый заголовок в `index.html` исправлен
   - ✅ Добавлен fallback "United Crypto Boys" в функцию `setTitle`
   - ⚠️ Динамическое обновление заголовка зависит от `getFinalSEO()` из Readymag
   - Если `getFinalSEO()` возвращает пустое значение, будет использован fallback "United Crypto Boys"

---

## 🎯 Результат

После применения исправлений:

- ✅ Базовый заголовок изменен с "UCB" на "United Crypto Boys"
- ✅ Добавлен fallback "United Crypto Boys" в функцию `setTitle`
- ✅ Цифры в URL скрыты (если нет `pagePath` или `uri`, URL будет просто `/`)
- ✅ Заголовок "Project" больше не должен появляться (используется fallback)

## 📝 Технические детали

### Как работает исправление URL:

**Было**:
```javascript
// Для страницы 2 без pagePath/uri:
n.num!==1 → true
e += (undefined || undefined || "2") + "/"
// Результат: URL = "/2/"
```

**Стало**:
```javascript
// Для страницы 2 без pagePath/uri:
n.num!==1 → true
(n.pagePath||n.uri) → false (оба undefined)
// Условие не выполняется, номер не добавляется
// Результат: URL = "/"
```

### Как работает исправление заголовка:

**Было**:
```javascript
document.title = a.title || ""
// Если title пустой → document.title = ""
// Браузер может показать дефолтное значение или "Project"
```

**Стало**:
```javascript
document.title = a.title || "United Crypto Boys"
// Если title пустой → document.title = "United Crypto Boys"
// Всегда есть правильное значение
```

---

*Дата создания анализа: 2026-01-15*  
*Дата исправлений: 2026-01-15*  
*Версия: 1.1*
